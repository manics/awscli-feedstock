diff --git a/setup.py b/setup.py
index e2be7adbb..46507ce2f 100644
--- a/setup.py
+++ b/setup.py
@@ -45,9 +45,9 @@ setup_options = dict(
     long_description=read('README.rst'),
     author='Amazon Web Services',
     url='http://aws.amazon.com/cli/',
-    scripts=['bin/aws', 'bin/aws.cmd', 'bin/aws_legacy_completer',
-             'bin/aws_completer', 'bin/aws_zsh_completer.sh',
+    scripts=['bin/aws_completer', 'bin/aws_zsh_completer.sh',
              'bin/aws_bash_completer'],
+    entry_points={"console_scripts": ["aws = awscli.clidriver:main"]},
     packages=find_packages(exclude=['tests*']),
     include_package_data=True,
     install_requires=requires,
